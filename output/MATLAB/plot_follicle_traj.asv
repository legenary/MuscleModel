clear;
ca;

fol_traj = zeros(31, 120, 6);
for fol = 1:31
    filename = sprintf("../fol_pos/fol_%02d.csv", fol-1);
    fol_traj(fol, :, :) = load(filename);
    
end

figure; hold on;
for i = 1:3
plot3d(squeeze(fol_traj(1, :, 1:3)), 'k-');
plot3d(squeeze(fol_traj(1, :, 4:6)), 'k-');
axis equal